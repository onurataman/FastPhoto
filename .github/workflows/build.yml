name: Build FastPhoto APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Decode Release Keystore
      env:
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
      run: |
        mkdir -p keystore
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > keystore/fastphoto-release.jks
        echo "=== KEYSTORE DIAGNOSTICS ==="
        ls -lh keystore/
        echo "Bytes: $(wc -c < keystore/fastphoto-release.jks) (expected ~2700)"
        echo "SHA256: $(sha256sum keystore/fastphoto-release.jks | cut -d' ' -f1)"
        echo "Password length: ${#KEYSTORE_PASSWORD} chars (expected 32)"
        echo "Alias: '$KEY_ALIAS' (expected 'fastphoto')"
        # Validate keystore — fails fast with clear msg if corrupt or wrong password
        keytool -list -keystore keystore/fastphoto-release.jks -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS"

    - name: Build Release APK
      env:
        KEYSTORE_FILE: ${{ github.workspace }}/keystore/fastphoto-release.jks
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      run: ./gradlew assembleRelease --stacktrace

    - name: Rename APK for Production Look
      run: mv app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/FastPhoto.apk

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: FastPhoto
        path: app/build/outputs/apk/release/FastPhoto.apk

    - name: Delete previous 'latest' release
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      run: gh release delete latest --yes --cleanup-tag || true
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Publish 'latest' release
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: latest
        name: Latest Build
        body: |
          Auto-generated latest release build from main.
          Properly signed release APK (~10MB, no debug-key warnings).
          Direct download: https://github.com/onurataman/FastPhoto/releases/latest/download/FastPhoto.apk
        files: app/build/outputs/apk/release/FastPhoto.apk
        make_latest: true
        prerelease: false
